# Main website routing
/* /index.html 200

# Handle static assets
/assets/* /assets/:splat 200

# Handle service worker
/service-worker.js /service-worker.js 200

# Handle manifest
/manifest.json /manifest.json 200

# Handle icons
/icons/* /icons/:splat 200

# Handle fonts
/fonts/* /fonts/:splat 200

# Handle images with proper MIME types and iOS support
/assets/images/*.jpg /assets/images/:splat 200
/assets/images/*.jpeg /assets/images/:splat 200
/assets/images/*.png /assets/images/:splat 200
/assets/images/*.webp /assets/images/:splat 200
/assets/images/*.svg /assets/images/:splat 200
/assets/images/*.ico /assets/images/:splat 200
/assets/images/*.gif /assets/images/:splat 200

# Essential files
/favicon.ico /favicon.ico 200
/robots.txt /robots.txt 200
/sitemap.xml /sitemap.xml 200

# Error pages
/404.html /404.html 404
/500.html /500.html 500

# Prevent directory listing
/*/ /index.html 200

# iOS specific image handling
/assets/images/* /assets/images/:splat 200
